#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point.
 * Description: Program that assign a random number each time executed
 * Return: 0
 */
int main(void)
{
	char k;

	for (k = 'a' ; k <= 'z'; k++ "\n")
		putchar(k);

	return (0);
}
