#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*
 * main - entry point
 * Description: program will assign a random number to the variable n each
 * Return: 0
 */
int main(void)
{
	int n;
       	int F;
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (F == 0)
	{
		printf("F of %d is %d and is 0\n", F, n);
	}
	else if (F > 5)
	{
		printf("F of %d is %d and is greater than 5\n, F, n"
	}
	else if (F > 0 < 6)
	{
	printf("F of %d is %d and is less than 6 and not 0\n", F, n);
	}
	return (0);
}
