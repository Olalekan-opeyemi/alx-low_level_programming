#include "main.h"

/**
 * _abs - Make a function that computes the absolute value of an integer.
 * @num: absolute value
 * Return: num
 */
int _abs(int num)
{
	if (num < 0)
	{
	return (-1 * num);
	}
	else
	{
	return (num);
	}
}
